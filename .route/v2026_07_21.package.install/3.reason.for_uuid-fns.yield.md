# reason: uuid-fns@1.1.3

## package
- name: uuid-fns
- version: 1.1.3
- type: prod

## reason
mint opaque equality-only version tokens as uuids instead of an autoincrement counter — removes ordering temptation (uuids are unorderable by construction) and removes the mutable per-instance counter state (stateless minting, no need to track the prior version)
