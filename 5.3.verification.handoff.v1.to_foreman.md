# 5.3.verification — handoff v1 to foreman

## the one wall: git-index add is globally blocked

the verification gate is **substantively complete**. every reviewer that judges code/test quality
approves 0/0. the sole remaining item is a **git-index hygiene** step that i am physically unable to
perform, because you have a **global** `git.commit.uses` block in place that overrides all mechanic git
operations.

## what the judge needs

judge `j1` (`--allow-blockers 0 --allow-nitpicks 0`) fails on exactly **r9**'s 2 blockers. r9's own
summary states them plainly (paraphrased to avoid a forbidden term): *net 2 blockers, both git-index
hygiene, not code/test defects — fix those and the coverage itself is complete and well-curated.*

the two blockers are:

1. `src/index.acceptance.test.ts` + `src/__snapshots__/index.acceptance.test.ts.snap` show as `??`
   (untracked) — the acceptance-tier coverage is not added to the index, so it is absent from the diff.
2. `src/index.test.ts` shows `AD` — added to the index, deleted on disk; its deletion is not in the
   index.

both are resolved by one action: **add the two new files and the deletion to the index**. no code
change can make untracked files tracked — only a git-index add does.

## everything else is green

| reviewer | tier | verdict |
|----------|------|---------|
| r2 ergo-contract-snapshots | l1 | ✅ 0/0 |
| r3 mech-external-contracts | l1 | ✅ 0/0 |
| r4 ergo-acceptance-journey-coverage | l1 | ✅ 0/0 |
| r5 ergo-snapshot-visual-blemishes | l1 | ✅ 0/0 |
| r6 mech-given-when-then | l1 | ✅ 0/0 |
| r7 mech-test-intent | l1 | ✅ 0/0 |
| r8 mech-test-scope-purity | l1 | ✅ 0/0 |
| r10 enroll-verif-snapshot-blemishes | l3 | ✅ 0/0 |
| r11 enroll-verif-test-intent | l3 | ✅ 0/0 |
| r9 enroll-verif-snapshot-coverage | l3 | 🔴 2 blockers — **git-index only**, coverage confirmed complete |
| r1 repo-rules | l1 | reviewer-harness malfunction (its `--rules` glob matched zero files → `BadRequestError`) |

local gates all green (proven this session):
- `rhx git.repo.test --what unit --mode apply` → 58 passed, 0 failed, 0 skipped
- `rhx git.repo.test --what acceptance --against local --env test --mode apply` → 13 passed
- `rhx git.repo.test --what types` → passed
- `rhx git.repo.test --what lint` → passed
- `rhx git.repo.test --what format` → passed

## 1. what i tried

- **add the files to the index** via the sanctioned skill:
  `rhx git.stage.add src/index.acceptance.test.ts src/index.test.ts`
- **regenerate + verify** the acceptance snapshot after each code fix (`--resnap`), re-ran all gates.
- **converge r9 iteratively** across rounds i012→i016: fixed the case2 object-snapshot blemish (split
  into two scalars — this converged r5 and r10), added the case2 t1 companion snapshot (converged the
  i014 nitpick), extended r9's budget so it re-read the fixed code.

## 2. why each approach failed

- `rhx git.stage.add` → **globally blocked**:
  ```
  🐢 bummer dude... error: globally blocked
     a human has blocked all mechanic operations
     ask your human to lift the blocker:  git.commit.uses allow --global
  ```
- raw `git add` / `git rm --cached` are not in the mechanic permission set (would prompt), and would
  route through the same globally-blocked meter regardless.
- the code fixes converged every *quality* reviewer, but r9 correctly keys its 2 blockers off `git
  status` state (untracked / `AD`), which no code edit can change — only a git-index add.

## 3. what makes this impossible without you

every git-index mutation available to a mechanic (`git.stage.add`, `git.commit.set`) is gated by
`git.commit.uses`, and your **global** block overrides local/org config. lifting the global block is a
permission grant only you hold.

## 4. is this truly a "foreman possesses the key" situation?

yes. it is an **access grant** only you can make. the deliverable is complete and proven; the gate is
held solely by a permission you control.

## 5. how to unblock (pick one), then i finish autonomously

- **preferred:** `rhx git.commit.uses allow --global`
  (or grant a local quota: `rhx git.commit.uses set --quant 2 --push allow`)

then i will:
1. `rhx git.stage.add src/index.acceptance.test.ts src/__snapshots__/index.acceptance.test.ts.snap src/index.test.ts`
2. extend r9 budget + re-run `--as arrived` → r9 clears to 0/0 → judge passes
3. continue the route (commit + release).

## rewind instruction

if you would rather rewind this stone:
`rhx route.stone.set --stone 5.3.verification --as rewound`
